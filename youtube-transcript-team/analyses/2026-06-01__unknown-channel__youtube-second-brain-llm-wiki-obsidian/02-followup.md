# Follow-up

## 다음 작업 후보
- HTML 시각화의 각 단계에 실제 예시 노트 파일명을 연결한다.
- `raw / wiki / index / queries` 폴더 템플릿을 만들어 실제 세컨드 브레인 하네스로 확장한다.
- 같은 스크립트로 `RAG vs LLM Wiki` 비교표만 따로 만든다.

## 같이 보면 좋은 영상 유형
- Obsidian graph view 기본 사용법
- Claude Code/Codex로 markdown 지식 베이스 정리하는 데모
- RAG의 chunking, embedding, semantic search 입문
- 지식 하네스 설계와 검증 루프 사례

## 남은 리스크
- 첨부 스크립트는 후반부 구간만 제공되어 영상 전체의 서론과 전제는 일부 추정이다.
- 데모 화면 자체를 보지 않았으므로 UI 위치, 생성 파일 수, 실제 품질은 스크립트 기준으로만 판단했다.

