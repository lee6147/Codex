# Follow-up

## 바로 실행할 것
- 현재 자주 실패하는 작업 하나를 정해서 Claude와 Codex에 같은 조건으로 비교 실행해라.
- `테스트는 절대 임의 수정 금지`, `파일 최대 길이`, `오류 시 사용자 질의` 규칙을 다시 정리해라.
- 구현과 검토를 분리해서 `구현=Claude/Codex`, `검토=Codex` 같은 조합을 실제로 측정해라.

## 같이 보면 좋은 영상
- Claude Code 설정 최적화 영상
- Codex 실사용 비교 영상
- AGENTS.md / 규칙 파일 설계 영상

## 다음 비교 후보
- Claude 구현 + Codex 리뷰 vs Codex 구현 + Codex 리뷰
- 규칙 파일 길이와 준수율의 상관관계
- 테스트 코드 보호 전략과 승인 플로우

## 메모
- 발표자의 결론은 꽤 강하지만, 핵심은 “Claude를 버릴까”보다 “신뢰 비용까지 포함하면 어떤 도구가 더 싼가”라는 질문이다.
- 우리 쪽에선 이걸 도구 취향 문제가 아니라 `후처리 비용`, `품질 유지 비용`, `규칙 위반 비용` 문제로 읽는 게 더 정확하다.
