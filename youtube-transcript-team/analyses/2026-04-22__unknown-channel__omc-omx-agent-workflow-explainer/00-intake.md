# Intake

## 기본 정보
- 작업일: 2026-04-22
- 영상 주제: OMC, OMX, Claude Code, Codex, AI agent workflow, 메모리 시스템, 팀 런타임
- 채널명: 확인 불가
- 영상 형식: 인터뷰 / 팟캐스트형 대화
- 사용자 요청: "아래 유튜브 스크립트를 보고 omc,omx에 대해 상세히 분석해서 설명서 만들어!!"

## 입력 신뢰도
- 수준: 보통
- 이유:
  - 사용자 제공 원문은 매우 길고 거의 전체 흐름을 담고 있다.
  - 다만 자동 자막/전사 특유의 오탈자와 발화 혼선이 많다.
  - 제품 문서, 코드, README, 공식 발표문이 아니라 인터뷰 스크립트이므로 자기 해석과 홍보성 표현이 섞여 있을 수 있다.

## 스크립트 상태
- 제공 형태: 사용자 메시지에 직접 포함된 장문 전사
- 범위: 약 1시간 27분 분량 인터뷰 전체에 가까운 흐름
- 주의:
  - "Claw Code", "오픈 클로", "클로입", "가제", "랄플랜" 등은 전사 오염 가능성이 있다.
  - 스크립트 안의 "오늘은 4월 8일"은 녹화 시점에 대한 내부 단서이지만 연도는 이 원문만으로 확정하기 어렵다.

## 이번 분석의 초점
- OMC와 OMX를 무엇으로 정의할 수 있는가
- 두 도구가 해결하려는 실제 문제는 무엇인가
- 메모리 시스템, 인터뷰, 프리모템, 팀 런타임이 어떤 방식으로 연결되는가
- 어디까지 자동화가 가능하고, 어디서 인간의 개입이 꼭 필요한가
- 초보자/실무자/팀 리드가 각각 어떤 관점으로 이해해야 하는가

## 아카이브 메모
- 본 산출물은 transcript 기반 해설 문서다.
- 원문 전체는 대화 스레드에 제공되었다.
- 사용자 요청에 따라 파일 수를 줄이기 위해 별도 `transcript/` 파일들을 유지하지 않고, 재사용에 필요한 구간 요약을 이 파일 안으로 통합했다.
- 구조:
  - `01-analysis.md`: 분석 + 설명서 본문
  - `02-followup.md`: 도입용 체크리스트 / 빠른 사용 가이드

## 통합 구간 요약

### 0:00-9:24
- 인터뷰 도입부에서 화자는 `OMC`, `OMX`, `AI agent workflow`가 최근 뜨거운 주제라고 말한다.
- 개인적으로는 컨트레이딩 자동화와 장시간 agent harness 실험에서 출발했고,
  그 경험을 코딩으로 옮기며 `오케스트레이터는 delegation`, `하위 agent는 execution`을 맡는 구조를 강조한다.
- 이 초반부에서 이미 "모델보다 하네스"라는 관점의 씨앗이 나온다.

### 11:19-20:39
- 내부 agent 운영 사례를 통해 메모리, 채널 라우팅, 세션 관리의 중요성을 설명한다.
- Discord/Slack 채널을 메모리와 작업 라우팅 공간처럼 쓰는 구조를 말하며,
  GitHub issue -> daemon -> 적절한 채널 -> agent activation -> 세션 실행의 흐름을 예시로 든다.
- 사람 역할은 구현보다 디렉팅과 운영 조율 쪽으로 이동한다고 본다.

### 20:45-35:55
- 메모리 시스템은 거대한 런타임이 아니라, 무엇을 언제 기록하고 언제 재정리할지에 대한 운영 프로토콜에 가깝다고 설명한다.
- `learnings` 같은 계층에 실패와 교훈을 쌓고, 주기적으로 중복 제거/재구조화를 거쳐 더 영속적인 행동 규칙으로 다듬는 흐름을 말한다.
- "디지털 아기" 비유를 통해 agent는 방치하면 저절로 좋아지는 존재가 아니라, 잘못 배우면 계속 교정해야 하는 대상이라고 본다.

### 36:31-49:06
- OMC를 본격적으로 소개하는 핵심 구간이다.
- OMC는 `Claude Code 위에 얹는 풀옵션 하네스`로, 유용한 기능과 스킬을 한데 묶고, 프리셋과 키워드 감지로 학습곡선을 낮추려는 방향으로 설명된다.
- OMX는 `Codex 중심의 team runtime`으로, 재사용되는 worker lane, mailbox, worktree, 동적 재할당 같은 협업 런타임 관점이 강조된다.
- 이 구간은 이번 강의에서 가장 중요한 개념 소개 구간이다.

### 49:06-53:01
- 왜 공식 제공 도구가 아닌 별도 하네스가 의미가 있는지 설명한다.
- 모델 프로바이더는 토큰 효율과 안정성을 우선하지만,
  하네스 제작자는 인지 부하 감소와 결과물 품질을 위해 더 공격적인 튜닝을 할 수 있다는 논리다.
- 공식 툴과 하네스의 차이를 "제조사 기본 세팅 vs 현장 튜닝"으로 비유한다.

### 54:21-1:15:26
- OMX 데모와 함께 실제 워크플로우가 나온다.
- `Deep Interview -> Ralplan -> Team Mode/Ralph` 흐름,
  프리모템, planning/execution 분리, worktree 기반 팀모드, auto-commit/fast-forward/rebase/conflict nudge, 메시지 telemetry가 핵심이다.
- 단순 subagent 병렬 실행이 아니라 "중간중간 소통하는 팀"이 중요하다는 주장이 가장 선명하게 드러난다.

### 1:15:26-끝
- 외부 채널 라우팅, 행사, 철학, 운영 태도에 대한 마무리 대화가 이어진다.
- 전체 인터뷰는 큰 이론보다 "실제로 굴러가게 만드는 운영감각"을 더 중시하는 방향으로 끝난다.
